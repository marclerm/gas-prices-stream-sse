services:
  api:
    build:
      context: ./GasApi
    container_name: gas-api
    expose:
      - "5000"
    environment:
      - ASPNETCORE_URLS=http://+:5000

  web:
    build:
      context: ./GasFrontend
    container_name: gas-web
    depends_on:
      - api
    ports:
      - "8080:80"
